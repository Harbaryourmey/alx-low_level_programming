#include <stdio.h>
/**
* main - Entry point
*
* Description: To print all possible combinations of two two-digit numbers.
*
* Return: Always 0 (Success)
*/
int main(void)
{
int m, n;
for (m = 0; m <= 99; m++)
for (n = 0; n <= 99; n++)
if (m < n  && m != n)
{
putchar((m / 10) + '0');
putchar((m % 10) + '0');
putchar(' ');
putchar((n / 10) + '0');
putchar((n % 10) + '0');
}
if (m != 98 || n != 99)
{
putchar(',');
putchar(' ');
}
putchar('\n');
return (0);
}
{
	int i, j;

	for (i = 0; i <= 99; i++)
	{
		for (j = 0; j <= 99; j++)
		{
			if (i < j && i != j)
			{
				putchar((i / 10) + '0');
				putchar((i % 10) + '0');
				putchar(' ');
				putchar((j / 10) + '0');
				putchar((j % 10) + '0');
				if (i != 98 || j != 99)
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar('\n');
return (0);
}