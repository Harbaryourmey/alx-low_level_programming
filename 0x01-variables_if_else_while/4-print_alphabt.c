#include <stdio.h>
/**
* main - Entry point
*
* Description: To prints the alphabet in lowercase, followed by a new line.
*
* Return: Always 0 (Success)
*/
int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
if (ch != 'q' && ch != 'e')
else
{putchar(ch); }
putchar('\n');
return (0);
}
