#include <stdio.h>
/**
* main - Entry point
*
* Description: To print all single digit numbers of base 10 starting from 0.
*
* Return: Always 0 (Success)
*/
int main(void)
{
int y;
for (y = 0; y < 10; y++)
printf("%d", y);
putchar('\n');
return (0);
}
